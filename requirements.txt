# ─── 3D AI Content Pipeline — Dependencies ───────────────────────
# NOTE: bpy is NOT installed via pip; it's Blender's bundled Python.
# NOTE: FFmpeg must be installed system-wide (brew install ffmpeg).

# LLM — G4F (free) + Gemini + OpenAI (fallback)
g4f>=0.4.0
google-genai>=1.0.0
openai>=1.0.0

# Voice — Kokoro TTS
kokoro>=0.7.0
soundfile>=0.12.0
numpy>=1.24.0

# Subtitles — Whisper
openai-whisper>=20231117

# Image handling
Pillow>=10.0.0

# Asset download (CMU library)
huggingface_hub>=0.20.0
